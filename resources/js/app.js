import './bootstrap';
import 'bootstrap-icons/font/bootstrap-icons.css';
import './goBack';

// window.Echo = new Echo({
    
//     broadcaster: "pusher",
//     key: import.meta.env.VITE_PUSHER_APP_KEY,
//     cluster: import.meta.env.VITE_PUSHER_APP_CLUSTER,
//     forceTLS: true
// });

// Subscribe to the 'chat' channel
// window.Echo.channel('chat')
//     .listen('App\\Events\\MessageSent', (e) => {
//         console.log('Message received:', e); // Log the received event data

        // Add the new message to the chat UI or perform any other desired action
        // For example, you can use Vue.js, React, or vanilla JavaScript to update the chat UI
    // });